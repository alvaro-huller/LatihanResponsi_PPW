DROP DATABASE IF EXISTS bioskop;
CREATE DATABASE bioskop;
USE bioskop;

CREATE TABLE film (
    id_film INT AUTO_INCREMENT PRIMARY KEY,
    judul_film VARCHAR(255) NOT NULL,
    sutradara VARCHAR(255) NOT NULL,
    harga_tiket INT NOT NULL
);

CREATE TABLE users (
    id_user INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    nama_lengkap VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Insert default admin user
INSERT INTO users (username, password, nama_lengkap) VALUES
('admin', 'admin', 'Administrator');

-- Insert sample data film
INSERT INTO film (judul_film, sutradara, harga_tiket) VALUES
('Pengkhianat Jed', 'Hanung Bramantyo', 75000),
('Filosofi Kopi', 'Angga Dwimas Sasana', 95000);